"""Unicode identifiers and strings — must not break the parser."""

def calcular_precio(cantidad: int) -> int:
    """Spanish identifiers."""
    return cantidad * 900


def 计算价格(数量: int) -> int:
    """Chinese identifiers, legal in Python 3."""
    return 数量 * 6000


EMOJI_TABLE = {"rocket": "🚀", "fire": "🔥", "check": "✅"}
LONG_LINE = "x" * 3000
